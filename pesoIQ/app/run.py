def main() -> None:
    """Minimal CLI entry point.

    This is intentionally lightweight to avoid adding features while
    providing a stable target for entry points.
    """
    # Placeholder: keep output minimal and non-invasive
    print("pesoIQ: ready. (no-op)")
